apiVersion: apps/v1
kind: Deployment
metadata:
  name: can-ingestion-service
  namespace: connected-vehicle
  labels:
    app: can-ingestion-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: can-ingestion-service
  template:
    metadata:
      labels:
        app: can-ingestion-service
    spec:
      containers:
        - name: can-ingestion-service
          image: connected-vehicle/can-ingestion-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: http
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: can-ingestion-service
  namespace: connected-vehicle
  labels:
    app: can-ingestion-service
spec:
  type: ClusterIP
  selector:
    app: can-ingestion-service
  ports:
    - port: 8081
      targetPort: 8081
      name: http
