apiVersion: apps/v1
kind: Deployment
metadata:
  name: fleet-analytics-service
  namespace: connected-vehicle
  labels:
    app: fleet-analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fleet-analytics-service
  template:
    metadata:
      labels:
        app: fleet-analytics-service
    spec:
      containers:
        - name: fleet-analytics-service
          image: connected-vehicle/fleet-analytics-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
              name: http
          envFrom:
            - configMapRef:
                name: platform-config
            - secretRef:
                name: platform-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: fleet-analytics-service
  namespace: connected-vehicle
  labels:
    app: fleet-analytics-service
spec:
  type: ClusterIP
  selector:
    app: fleet-analytics-service
  ports:
    - port: 8085
      targetPort: 8085
      name: http
